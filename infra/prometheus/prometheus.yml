global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agentscope-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:8080]

  - job_name: agentscope-runtime
    metrics_path: /metrics
    static_configs:
      - targets: [runtime:8000]
